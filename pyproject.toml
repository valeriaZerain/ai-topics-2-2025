[project]
name = "ai-topics-2-2025"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "numpy>=2.3.3",
    "pillow>=11.3.0",
]

[tool.uv.workspace]
members = [
    "2.tools",
]
